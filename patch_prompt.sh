sed -i 's/* Construct a beautiful, clean Markdown Table with the columns: | Complaint ID | User | Department | Priority | Age (Days) | Description |./* Use the `structuredData` JSON property to render an interactive HTML DataTable. DO NOT output raw Markdown tables. Set structuredData.type = "table" and provide columns and rows./g' server.ts
sed -i 's/* Count and categorize active tickets. Construct a beautiful Markdown Table showing: | Department | Total Tickets | Pending | In Progress | Resolved |./* Count and categorize active tickets. Use the `structuredData` JSON property to render an interactive HTML DataTable instead of a Markdown table./g' server.ts
sed -i 's/  * Generate reports, metrics summaries, tables, lists, and workflow diagrams based on the live database records./  * Generate reports, metrics summaries, tables, lists, and workflow diagrams based on the live database records. IMPORTANT: For tabular data, charts, and KPI summary cards, ALWAYS populate the `structuredData` JSON property. Never use Markdown tables./g' server.ts
